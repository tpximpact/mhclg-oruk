# Local development overrides
# Copy this to .env.local and fill in your values.
# NOTE: Do not commit .env.local to version control.
USE_COOKIES=0
USE_NAV=1
USE_NOWARRANTY=0

# API endpoints
DASHBOARD_ENDPOINT=
DASHBOARD_DETAILS_ENDPOINT=
VALIDATOR_ENDPOINT=
REGISTER_ENDPOINT=

# Full connection string, e.g. for MongoDB Atlas or local MongoDB
# Examples:
# mongodb://localhost:27017
# mongodb+srv://<user>:<password>@<cluster>.mongodb.net
MONGODB_URI=

# Default database name to use when not specified explicitly
MONGODB_DB=oruk

# GitHub integration for automatic issue creation
# Required for creating verification issues when new services are registered
GITHUB_OWNER=tpximpact
GITHUB_REPO=mhclg-oruk
GITHUB_TOKEN=

# Base URL for the application (used in GitHub issue links)
NEXTAUTH_URL=http://localhost:3000
